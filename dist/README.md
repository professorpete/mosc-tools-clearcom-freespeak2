# Built module package

`clearcom-freespeak2-1.0.0.tgz` is the installable Companion module package.

**To install:** download it, then in Companion 3.4+ go to
**Connections → Import module package** and select the file.

Rebuild it yourself with `npm install && npx companion-module-build`.
